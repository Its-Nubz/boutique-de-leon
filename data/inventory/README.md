# Inventory data

- `current-inventory.js` contains newer products and authoritative corrections that are not maintained in the original base catalog.
- `products.js` at the repository root remains the base catalog for now.
- Product descriptions are maintained separately under `data/descriptions/` by storefront category.

When adding or correcting inventory, update the existing product record instead of creating dated patch files.
